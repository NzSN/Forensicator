\* Trace generation for mbt_snapshot.rs anchors on the cheap cursor-local
\* invariant; SnapshotsAreModels (the \A t <= frontier property) stays the
\* offline deep check (Snapshot.cfg, ~30 min at depth 10).
SPECIFICATION MBTSpec
INVARIANT SnapshotValid
